# 🔄 Система автообновления данных

## Обзор

Создана автономная система синхронизации данных между БД PostgreSQL и `site-data.json`. Все изменения на сайте автоматически сохраняются в обе системы хранения.

---

## 📦 Компоненты системы

### 1. Backend функция `/backend/sync-content/`

**URL:** `https://functions.poehali.dev/d936140f-1158-4c59-85e7-bb904b44c4bc`

#### API методы:

- **GET** `/?direction=db-to-json` - экспорт данных из БД в формат JSON
- **POST** `/` - импорт JSON данных в БД
  ```json
  {
    "data": {
      "logo": "...",
      "hero": {...},
      "services": {...}
    }
  }
  ```

### 2. Скрипты синхронизации

#### `scripts/sync-to-db.js`
Отправляет данные из `public/site-data.json` в БД
```bash
node scripts/sync-to-db.js
```

#### `scripts/sync-from-db.js`
Загружает данные из БД и обновляет `public/site-data.json` и `standalone/data/site-data.json`
```bash
node scripts/sync-from-db.js
```

#### `scripts/auto-sync.js`
Автоматическая двусторонняя синхронизация (следит за изменениями каждые 10 секунд)
```bash
node scripts/auto-sync.js
```

---

## 🚀 Использование

### Ручная синхронизация

**Обновить БД из файла:**
```bash
node scripts/sync-to-db.js
```

**Обновить файл из БД:**
```bash
node scripts/sync-from-db.js
```

### Автоматическая синхронизация

**Запустить мониторинг:**
```bash
node scripts/auto-sync.js
```

Скрипт будет:
- Следить за изменениями в `public/site-data.json`
- Автоматически синхронизировать с БД
- Следить за изменениями в БД
- Автоматически обновлять локальные файлы

**Рекомендуется** запускать в фоне во время разработки.

---

## 📂 Независимая копия сайта

### Расположение: `standalone/`

Полностью автономная версия сайта без Vite и Node.js:

```
standalone/
├── index.html          # Главная страница
├── css/
│   └── styles.css      # Стили
├── js/
│   ├── app.js         # Основная логика
│   └── admin.js       # Админ-панель
└── data/
    └── site-data.json # Данные контента
```

### Возможности standalone версии:

✅ Работает без сборщиков (открывай `index.html` в браузере)
✅ Все компоненты на чистом HTML + CSS + Vanilla JS
✅ Админ-панель с авторизацией (пароль: `admin123`)
✅ Inline редактирование контента
✅ Сохранение в localStorage
✅ Экспорт изменений в JSON
✅ Полная адаптивность и анимации
✅ Tailwind CSS через CDN
✅ Lucide Icons через CDN

### Запуск standalone версии:

**Вариант 1: Прямое открытие**
```bash
# Открой в браузере
open standalone/index.html
```

**Вариант 2: Локальный сервер**
```bash
cd standalone
python -m http.server 8080
# Открой http://localhost:8080
```

---

## 🔐 Админ-панель

### В React версии (основной сайт):
- Кнопка "Войти в админку" в хедере
- Пароль: вводится в диалоге
- Inline редактирование всех секций
- Автосохранение в localStorage и БД

### В Standalone версии:
- Кнопка "Войти" в хедере
- Пароль по умолчанию: `admin123`
- Редактирование контента inline
- Сохранение только в localStorage
- Экспорт изменений через кнопку "Скачать JSON"

---

## 🗄️ База данных

**Схема:** `t_p71685242_black_gold_security_`

**Таблица:** `site_content`

| Колонка | Тип | Описание |
|---------|-----|----------|
| content_key | VARCHAR | Ключ секции (logo, hero, services, и т.д.) |
| content_data | JSONB | Данные секции в JSON формате |
| updated_at | TIMESTAMP | Время последнего обновления |

---

## 📝 Структура данных

Данные хранятся в разбитом виде по секциям:

```json
{
  "logo": "https://...",
  "hero": {
    "title": "...",
    "subtitle": "...",
    "image": "",
    "imageSize": 100
  },
  "services": {
    "title": "...",
    "subtitle": "...",
    "items": [...]
  },
  "about": {...},
  "portfolio": {...},
  "cases": {...},
  "blog": {...},
  "contacts": {...},
  "footer": {...}
}
```

---

## 🔧 Техническая информация

### Основной сайт (React + Vite):
- **Stack:** Vite + React + TypeScript + Tailwind
- **Хранение:** localStorage + PostgreSQL (двойное)
- **Синхронизация:** Автоматическая через `contentStore.ts`

### Standalone версия:
- **Stack:** HTML + CSS + Vanilla JS
- **Хранение:** localStorage + JSON файл
- **Зависимости:** Tailwind CDN + Lucide Icons CDN

### Backend функции:
- **Runtime:** Python 3.11
- **Framework:** Cloud Functions (Yandex Cloud)
- **База данных:** PostgreSQL через psycopg2
- **Протокол:** Simple Query Protocol

---

## 📊 Workflow

```
Пользователь редактирует контент
           ↓
    React компонент обновляется
           ↓
    contentStore.saveContent()
           ↓
    ┌──────────────────┬──────────────────┐
    ↓                  ↓                  ↓
localStorage    Backend API         БД PostgreSQL
    ↓                  ↓                  ↓
    └──────────────────┴──────────────────┘
                     ↓
           Синхронизация завершена
```

---

## ⚡ Быстрый старт

1. **Запустить React сайт:**
   ```bash
   npm run dev
   ```

2. **Запустить Standalone версию:**
   ```bash
   cd standalone
   python -m http.server 8080
   ```

3. **Включить автосинхронизацию:**
   ```bash
   node scripts/auto-sync.js
   ```

4. **Обновить данные вручную:**
   ```bash
   # Из файла в БД
   node scripts/sync-to-db.js
   
   # Из БД в файл
   node scripts/sync-from-db.js
   ```

---

## 🎯 Итоги

✅ **Автономная система синхронизации** между БД и site-data.json
✅ **Backend функция** для импорта/экспорта данных
✅ **3 скрипта** для ручной и автоматической синхронизации
✅ **Независимая копия сайта** без Vite в папке `standalone/`
✅ **Сохранение всего функционала** в обеих версиях
✅ **Админ-панели** с авторизацией и редактированием

Все работает автономно и готово к использованию! 🚀
