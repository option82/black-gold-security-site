# 🗄️ Система управления контентом через базу данных

## ✅ Готово к использованию!

Ваш сайт теперь работает с **базой данных PostgreSQL** и **автоматически синхронизируется** на всех устройствах!

---

## 🎯 Как это работает

### Автоматическая система
1. **Вы редактируете** контент через админ-панель
2. **Изменения сохраняются** в базу данных автоматически (через 1 секунду после редактирования)
3. **Все устройства** видят изменения мгновенно при обновлении страницы

### Fallback система
- Если БД недоступна → сайт загружает данные из `/public/site-data.json`
- Сайт **всегда работает**, даже если БД недоступна
- Белый экран исправлен — добавлена обработка ошибок

---

## 📝 Как редактировать контент

### 1. Войти в админ-панель
1. Откройте сайт
2. Нажмите на замок (🔒) в футере
3. Введите пароль: `Adminzachitabusinessa!QAZ`
4. Вы вошли в режим редактирования!

### 2. Редактировать контент
- **Наведите курсор** на любой блок
- Появится **кнопка редактирования** (карандаш)
- **Измените** текст, изображения, данные
- Изменения **автоматически сохранятся** в БД через 1 секунду

### 3. Проверить на другом устройстве
- Откройте сайт на телефоне/планшете/другом ПК
- **Обновите страницу** (F5 / потяните вниз)
- Все изменения видны! 🎉

---

## 🛠️ Техническая архитектура

### Frontend (React)
- Файл: `src/lib/contentStore.ts`
- Сначала пытается загрузить из **БД через API**
- Если БД недоступна → загружает из **статического файла**
- Кеширование в памяти для быстрой работы

### Backend API
- Функция: `backend/content/index.py`
- URL: `https://functions.poehali.dev/35863db1-bef4-47a1-93e6-5374942e52f5`
- Методы:
  - `GET /` - получить весь контент
  - `GET /?key=hero` - получить конкретный раздел
  - `POST /` - сохранить изменения (body: `{key, data}`)

### База данных
- Таблица: `t_p71685242_black_gold_security_.site_content`
- Колонки:
  - `id` - PRIMARY KEY
  - `content_key` - уникальный ключ раздела (hero, services, about...)
  - `content_data` - JSON с данными раздела
  - `updated_at` - время последнего обновления

---

## 🔄 Синхронизация между устройствами

### Сценарий 1: Редактирование на ПК → Просмотр на телефоне

```
1. ПК: Редактируете контент → автосохранение в БД (1 сек)
2. Телефон: Обновляете страницу → видите изменения
```

### Сценарий 2: Несколько редакторов

```
1. Редактор A: Изменил заголовок → сохранено в БД
2. Редактор B: Обновил страницу → видит изменения редактора A
3. Редактор B: Изменил услуги → сохранено в БД
4. Редактор A: Обновил страницу → видит изменения редактора B
```

### ⚠️ Конфликты
- Если два редактора изменяют **один и тот же раздел** одновременно
- Последнее сохранение **перезапишет** предыдущее
- Рекомендуется: координировать редактирование

---

## 🚀 Работа на любом хостинге

### Что нужно для работы:
1. ✅ **Статический хостинг** (Netlify, Vercel, GitHub Pages, и т.д.)
2. ✅ **PostgreSQL база данных** (Heroku, Supabase, Railway, и т.д.)
3. ✅ **Backend API** (уже развернут на poehali.dev)

### Как загрузить на хост:

#### Вариант 1: Build + Upload
```bash
# Собрать проект
npm run build

# Загрузить папку dist/ на любой хостинг
# Netlify, Vercel, GitHub Pages, Cloudflare Pages, etc.
```

#### Вариант 2: GitHub + Auto Deploy
```bash
# Push в GitHub
git add .
git commit -m "Updated content system"
git push

# Подключите GitHub к хостингу (Netlify/Vercel)
# Автоматический деплой при каждом push
```

### Настройка для работы без БД
Если хотите работать **только со статическими файлами**:

1. Отредактируйте `public/site-data.json`
2. Соберите проект: `npm run build`
3. Загрузите на хост
4. Сайт будет работать без БД (но без синхронизации изменений)

---

## 🔧 Миграция данных в БД

Если БД пуста, заполните её данными из `site-data.json`:

```bash
# Установить зависимости
pip install psycopg2-binary

# Экспортировать переменную окружения
export DATABASE_URL="your_database_url"

# Запустить миграцию
python scripts/migrate-to-db.py
```

Скрипт автоматически:
- Читает `public/site-data.json`
- Заполняет таблицу `site_content`
- Все разделы (hero, services, about...) будут в БД

---

## 🐛 Решение проблем

### Белый экран на хостинге
**Причина:** Ошибка загрузки данных не обрабатывалась

**Решение:** ✅ Исправлено!
- Добавлена обработка ошибок в `loadStoredContent()`
- Добавлен `ErrorBoundary` в `App.tsx`
- Fallback на статический файл если БД недоступна

### Изменения не видны на других устройствах
**Причина:** Кеш в браузере

**Решение:**
1. Обновите страницу (Ctrl+F5 / Cmd+Shift+R)
2. Очистите кеш браузера
3. Проверьте что БД работает: откройте URL API в браузере

### БД не сохраняет изменения
**Причина:** Ошибка подключения к БД

**Решение:**
1. Проверьте что backend функция работает
2. Откройте консоль браузера (F12)
3. Посмотрите ошибки при сохранении
4. Проверьте логи: `get_logs backend/content`

### API возвращает ошибку
**Причина:** Неправильная структура данных или ошибка SQL

**Решение:**
1. Проверьте логи: `get_logs backend/content`
2. Проверьте что таблица `site_content` существует
3. Проверьте формат данных в POST запросе

---

## 📊 Преимущества новой системы

| Функция | Старая система | Новая система |
|---------|---------------|---------------|
| Синхронизация устройств | ❌ Нет | ✅ Автоматическая |
| Зависимость от GitHub | ✅ Да | ❌ Нет |
| Работа без интернета | ✅ Да | ✅ Да (fallback) |
| Автосохранение | ❌ Нет | ✅ Да (1 сек) |
| Белый экран | ❌ Возможен | ✅ Исправлен |
| База данных | ❌ Нет | ✅ PostgreSQL |
| Работа на любом хостинге | ✅ Да | ✅ Да |

---

## 🎓 Дополнительная информация

### Структура разделов контента

```javascript
{
  "logo": "url_to_logo",
  "hero": { title, subtitle, image, imageSize },
  "about": { title, description, stats: [] },
  "services": { title, subtitle, items: [] },
  "portfolio": { title, items: [] },
  "cases": { title, items: [] },
  "blog": { title, posts: [] },
  "contacts": { title, contacts: [] },
  "footer": { text }
}
```

### API примеры

**Получить весь контент:**
```bash
curl https://functions.poehali.dev/35863db1-bef4-47a1-93e6-5374942e52f5
```

**Получить конкретный раздел:**
```bash
curl "https://functions.poehali.dev/35863db1-bef4-47a1-93e6-5374942e52f5?key=hero"
```

**Сохранить изменения:**
```bash
curl -X POST https://functions.poehali.dev/35863db1-bef4-47a1-93e6-5374942e52f5 \
  -H "Content-Type: application/json" \
  -d '{"key":"hero","data":{"title":"Новый заголовок","subtitle":"Новый подзаголовок"}}'
```

---

## 📞 Поддержка

- Telegram: https://t.me/+QgiLIa1gFRY4Y2Iy
- Email: support@poehali.dev
- Документация: https://docs.poehali.dev

---

© 2025 Poehali.dev - Создание сайтов через русский язык
