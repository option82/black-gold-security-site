# ⚡ Быстрый старт

## 🎯 Что у нас есть

1. **React версия** (`src/`) - основной сайт с админ-панелью
2. **Standalone версия** (`standalone/`) - HTML копия без зависимостей
3. **Система синхронизации** - автообновление БД и site-data.json

---

## 🚀 Запуск за 3 шага

### React версия:

```bash
npm run dev
# Откройте http://localhost:3000
# Войдите в админку → редактируйте контент
```

### Standalone версия:

```bash
cd standalone
python -m http.server 8080
# Откройте http://localhost:8080
# Войдите (пароль: admin123)
```

### Автосинхронизация:

```bash
node scripts/auto-sync.js
# Следит за изменениями каждые 10 секунд
# БД ↔ site-data.json
```

---

## 📝 Редактирование контента

### Через React админку:

1. `npm run dev`
2. Кнопка **"Войти в админку"** → введите пароль
3. Наведите на элемент → редактируйте
4. Автосохранение в localStorage + БД

### Через Standalone:

1. Откройте `standalone/index.html`
2. Кнопка **"admin"** → пароль `admin123`
3. Редактируйте inline
4. **"Скачать JSON"** → замените `data/site-data.json`

### Прямое редактирование файла:

1. Откройте `public/site-data.json`
2. Внесите изменения
3. Сохраните
4. `node scripts/sync-to-db.js` (отправить в БД)

---

## 🔄 Синхронизация

### Отправить в БД:

```bash
node scripts/sync-to-db.js
```

### Скачать из БД:

```bash
node scripts/sync-from-db.js
```

### Автоматически (рекомендуется):

```bash
node scripts/auto-sync.js
```

---

## 📂 Где что лежит

```
├── src/                      # React версия
│   └── components/
│       ├── AdminPanel.tsx    # Админ-панель
│       └── SyncManager.tsx   # UI синхронизации
├── standalone/               # HTML версия
│   ├── index.html           # Главная страница
│   └── data/site-data.json  # Данные
├── public/
│   └── site-data.json       # Исходные данные
├── backend/
│   ├── content/             # API контента
│   └── sync-content/        # API синхронизации
└── scripts/
    ├── sync-to-db.js        # JSON → БД
    ├── sync-from-db.js      # БД → JSON
    └── auto-sync.js         # Автосинхронизация
```

---

## 🎨 Кастомизация

### Изменить цвета:

**React:** `tailwind.config.ts` и `src/index.css`

**Standalone:** `standalone/index.html` (секция `tailwind.config`)

### Изменить пароль админки:

**React:** Вводится через UI при первом входе

**Standalone:** `standalone/js/admin.js` → `const ADMIN_PASSWORD`

---

## 🔥 Самые важные команды

```bash
# Разработка
npm run dev

# Автосинхронизация (фон)
node scripts/auto-sync.js

# Ручная синхронизация
node scripts/sync-to-db.js      # Файл → БД
node scripts/sync-from-db.js    # БД → Файл

# Standalone версия
cd standalone && python -m http.server 8080
```

---

## 🆘 Проблемы

### Контент не обновляется?

1. Очистите localStorage: `localStorage.clear()`
2. Перезагрузите страницу
3. Проверьте `node scripts/sync-from-db.js`

### Админка не открывается?

1. Проверьте пароль
2. Очистите cookies
3. Проверьте консоль браузера (F12)

### БД недоступна?

Система автоматически использует `site-data.json` как fallback.

---

## 📚 Подробная документация

- **DEPLOYMENT_GUIDE.md** - полное руководство
- **SYNC_README.md** - система синхронизации
- **standalone/README.md** - HTML версия

---

**Все готово к работе! 🚀**
