# 🚀 Руководство по развертыванию и синхронизации

## 📋 Содержание

1. [Архитектура проекта](#архитектура-проекта)
2. [Система хранения данных](#система-хранения-данных)
3. [Основной сайт (React + Vite)](#основной-сайт-react--vite)
4. [Standalone версия (HTML+CSS+JS)](#standalone-версия-htmlcssjs)
5. [Синхронизация данных](#синхронизация-данных)
6. [Workflow разработки](#workflow-разработки)
7. [FAQ](#faq)

---

## 🏗️ Архитектура проекта

Проект состоит из двух независимых версий:

```
project/
├── src/                    # React версия (основная)
│   ├── components/
│   ├── pages/
│   └── lib/
│       ├── storage.ts     # API для работы с данными
│       └── contentStore.ts # Логика кэширования и синхронизации
├── standalone/            # HTML версия (автономная)
│   ├── index.html
│   ├── css/
│   ├── js/
│   └── data/
├── backend/               # Облачные функции
│   ├── content/          # API контента (GET/POST)
│   └── sync-content/     # API синхронизации
├── public/
│   └── site-data.json    # Исходные данные
└── scripts/              # Утилиты синхронизации
    ├── sync-to-db.js     # JSON → БД
    ├── sync-from-db.js   # БД → JSON
    └── auto-sync.js      # Автоматическая двусторонняя синхронизация
```

---

## 💾 Система хранения данных

### Три уровня хранения:

1. **localStorage (браузер)**
   - Быстрый доступ
   - Локальные изменения
   - Работает оффлайн
   - Теряется при очистке браузера

2. **PostgreSQL (облако)**
   - Централизованное хранилище
   - Доступно из backend функций
   - Надежное и постоянное
   - Синхронизация через API

3. **site-data.json (статический файл)**
   - Fallback источник
   - Быстрая загрузка
   - Версионность через Git
   - Используется standalone версией

### Приоритет загрузки данных:

```
1. localStorage (если есть) → мгновенная загрузка
2. База данных → через API
3. site-data.json → статический файл
```

---

## 🎨 Основной сайт (React + Vite)

### Запуск разработки:

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для продакшна
npm run build
```

### Редактирование контента:

1. Откройте сайт в браузере
2. Нажмите кнопку **"Войти в админку"** в хедере
3. Введите пароль (устанавливается при первом входе)
4. Наведите на элемент для редактирования
5. Изменения сохраняются автоматически

### Компоненты системы:

#### `src/lib/contentStore.ts`
Управляет загрузкой и кэшированием данных:
```typescript
export const contentStore = {
  getAllContent(),   // Получить все данные
  getContent(key),   // Получить секцию
  saveContent(key, data)  // Сохранить секцию
}
```

#### `src/lib/storage.ts`
API высокого уровня:
```typescript
saveContent(content)      // Сохранить контент
loadContent()             // Загрузить контент
exportContent()           // Экспорт в JSON
downloadContentBackup()   // Скачать бэкап
```

#### `src/components/SyncManager.tsx`
UI для синхронизации с БД прямо из админ-панели.

---

## 📦 Standalone версия (HTML+CSS+JS)

### Расположение: `standalone/`

Полностью автономная версия без сборщиков.

### Запуск:

**Вариант 1: Прямое открытие**
```bash
open standalone/index.html
```

**Вариант 2: Локальный сервер**
```bash
cd standalone
python -m http.server 8080
# Откройте http://localhost:8080
```

**Вариант 3: На хостинге**
Загрузите папку `standalone/` на любой веб-хостинг:
- GitHub Pages
- Netlify
- Vercel
- Обычный PHP/Apache хостинг

### Особенности:

✅ Нет зависимостей (кроме Tailwind CDN + Lucide Icons CDN)
✅ Работает везде (даже на старых браузерах)
✅ Админ-панель встроена
✅ Сохранение в localStorage
✅ Экспорт изменений в JSON

### Редактирование:

1. Откройте standalone версию
2. Нажмите кнопку **"admin"** в футере
3. Введите пароль: `admin123`
4. Редактируйте контент inline
5. Скачайте изменения через **"Скачать JSON"**
6. Замените `standalone/data/site-data.json`

### Смена пароля админки:

Отредактируйте `standalone/js/admin.js`:
```javascript
const ADMIN_PASSWORD = 'ВашНовыйПароль';
```

---

## 🔄 Синхронизация данных

### Backend API:

#### `/backend/content/` (основной API)
- **URL:** `https://functions.poehali.dev/35863db1-bef4-47a1-93e6-5374942e52f5`
- **GET** `/` - получить все данные
- **GET** `/?key=hero` - получить секцию
- **POST** `/` - сохранить секцию

#### `/backend/sync-content/` (синхронизация)
- **URL:** `https://functions.poehali.dev/d936140f-1158-4c59-85e7-bb904b44c4bc`
- **GET** `/?direction=db-to-json` - экспорт из БД
- **POST** `/` - импорт в БД

### Скрипты синхронизации:

#### 1. Ручная синхронизация

**Отправить файл в БД:**
```bash
node scripts/sync-to-db.js
```
Читает `public/site-data.json` и отправляет в PostgreSQL.

**Скачать данные из БД:**
```bash
node scripts/sync-from-db.js
```
Загружает из PostgreSQL и обновляет:
- `public/site-data.json`
- `standalone/data/site-data.json`

#### 2. Автоматическая синхронизация

**Запустить мониторинг:**
```bash
node scripts/auto-sync.js
```

Что делает:
- Следит за изменениями `public/site-data.json`
- Автоматически отправляет в БД
- Следит за изменениями в БД
- Автоматически обновляет локальные файлы
- Проверка каждые 10 секунд

**Рекомендуется запускать в фоне во время разработки.**

#### 3. UI синхронизация

В админ-панели React версии доступен компонент `SyncManager`:
- **Файл → БД** - отправить site-data.json в БД
- **БД → Файл** - скачать актуальные данные из БД

---

## 🔄 Workflow разработки

### Сценарий 1: Редактирование через React админку

```
1. npm run dev
2. Открыть localhost:3000
3. Войти в админку
4. Редактировать контент
5. Контент автоматически сохраняется в:
   - localStorage (моментально)
   - БД через API (автоматически)
6. Использовать SyncManager для экспорта в файл
7. Закоммитить site-data.json в Git
```

### Сценарий 2: Редактирование через Standalone

```
1. Открыть standalone/index.html
2. Войти в админку (admin123)
3. Редактировать контент
4. Скачать изменения через кнопку
5. Заменить standalone/data/site-data.json
6. Опционально: node scripts/sync-to-db.js
7. Закоммитить в Git
```

### Сценарий 3: Прямое редактирование JSON

```
1. Открыть public/site-data.json
2. Внести изменения
3. Сохранить файл
4. node scripts/sync-to-db.js  (отправить в БД)
5. Закоммитить в Git
6. Обновить браузер (localStorage будет перезаписан из БД)
```

### Сценарий 4: Автоматическая синхронизация

```
1. node scripts/auto-sync.js  (запустить в фоне)
2. Редактировать public/site-data.json
3. Изменения автоматически попадают в БД
4. Или редактировать через админку
5. Изменения автоматически попадают в файл
6. Закоммитить финальную версию
```

---

## 🗄️ База данных

### Схема: `t_p71685242_black_gold_security_`

### Таблица: `site_content`

```sql
CREATE TABLE site_content (
  content_key VARCHAR PRIMARY KEY,
  content_data JSONB NOT NULL,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Структура данных:

Каждая секция хранится отдельной записью:

| content_key | content_data | updated_at |
|-------------|--------------|------------|
| logo | "https://..." | 2026-01-02 |
| hero | {"title": "...", "subtitle": "..."} | 2026-01-02 |
| services | {"title": "...", "items": [...]} | 2026-01-02 |
| about | {...} | 2026-01-02 |

### Преимущества такой структуры:

✅ Частичное обновление (только измененные секции)
✅ История изменений по секциям
✅ Быстрый доступ к отдельным блокам
✅ Оптимизация передачи данных

---

## 🔐 Безопасность

### Защита админ-панели:

**React версия:**
- Пароль вводится при первом входе
- Хранится в localStorage (хэшированный)
- Может быть изменен через UI

**Standalone версия:**
- Пароль захардкожен в `js/admin.js`
- Для production меняйте перед деплоем

### Рекомендации:

1. **Не коммитьте пароли** в Git
2. Используйте **переменные окружения** для production
3. Добавьте **rate limiting** для API в production
4. Настройте **HTTPS** на боевом сервере

---

## 📊 Мониторинг и отладка

### Проверка состояния:

```bash
# Проверить БД
curl https://functions.poehali.dev/35863db1-bef4-47a1-93e6-5374942e52f5

# Экспорт данных из БД
curl "https://functions.poehali.dev/d936140f-1158-4c59-85e7-bb904b44c4bc?direction=db-to-json"
```

### Логи:

```javascript
// В браузере
console.log('Content loaded from:', localStorage.getItem('site-data-source'));

// Backend логи
// Доступны через Yandex Cloud Console
```

---

## ❓ FAQ

### Q: Как откатить изменения?

**A:** Три варианта:
1. Очистить localStorage браузера → перезагрузить страницу
2. Откатить коммит с site-data.json в Git
3. Использовать `sync-from-db.js` для восстановления из БД

### Q: Что если БД недоступна?

**A:** Система автоматически использует fallback:
1. localStorage (если есть)
2. site-data.json (статический файл)

### Q: Можно ли использовать обе версии одновременно?

**A:** Да! Обе версии работают независимо:
- React версия: для управления и разработки
- Standalone: для простого хостинга

### Q: Как настроить автосинхронизацию в production?

**A:** Используйте PM2 или systemd:
```bash
pm2 start scripts/auto-sync.js --name site-sync
pm2 save
```

### Q: Нужно ли коммитить site-data.json?

**A:** Да! Это fallback источник и история изменений.

### Q: Как добавить новую секцию?

**A:**
1. Добавить в `public/site-data.json`
2. Обновить интерфейсы в `src/lib/storage.ts`
3. Создать компонент для отображения
4. Добавить в `src/pages/Index.tsx`

---

## 🎯 Чеклист запуска

### Dev окружение:

- [ ] `npm install`
- [ ] `npm run dev`
- [ ] Открыть localhost:3000
- [ ] Проверить админ-панель
- [ ] `node scripts/auto-sync.js` (опционально)

### Production:

- [ ] Настроить HTTPS
- [ ] Изменить пароль админки
- [ ] Проверить доступность БД
- [ ] Настроить автосинхронизацию
- [ ] Проверить standalone версию
- [ ] Настроить мониторинг

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте этот гайд
2. Посмотрите `SYNC_README.md`
3. Проверьте `standalone/README.md`
4. Проверьте логи в консоли браузера

---

**Автор:** Poehali.dev Team  
**Дата:** 02.01.2026  
**Версия:** 1.0.0
